# main.py — Entry point
# Implemented by peer headless/94d02251 (route state machine).
# This file is intentionally minimal on the imu branch to avoid merge conflicts.
# The full integration loop lives in that peer's main.py.

# imu.py and config.py are the deliverables of this branch (FR1).
# See imu.py for the IMU(i2c) class with calibrate() / update(now_ms) / heading().
